# از اینجا تا انتشار — فهرست کامل

وضعیت فعلی‌ات (از عکس‌هایی که فرستادی):

| چیز | وضعیت |
|---|---|
| سرور | ✅ بالا آمد |
| `applicationId` | ✅ `com.smahdi2razavi.pasur11` |
| `versionCode` | ✅ ۲ |
| تنظیمات درگاه در gradle | ✅ درست |
| فایل `app-release.apk` | ✅ ساخته شد (۱۹ مگابایت) |
| آپلود در مایکت | ⬜ مانده |

---

# بخش ۱ — سرور (۵ دقیقه، در VNC)

## ۱-۱) سرور را به‌روز کن
در VNC سرور این را کپی کن و Enter بزن:

```
bash <(curl -sL https://raw.githubusercontent.com/smahdi2razavi-code/Pasur11/claude/gallant-planck-ofe9g8/server/fix.sh)
```

آخرش باید بنویسد `RESULT: SERVER IS UP` و در بخش Feature check همه‌جا `OK`.

## ۱-۲) از مرورگر خودت چک کن
این آدرس را باز کن:
```
http://94.184.36.13/health
```
باید بنویسد `{"ok":true...}`

اگر این کار نکرد، بقیهٔ کارها را انجام نده — اول این باید درست شود.

---

# بخش ۲ — سه چیز را در اندروید استودیو چک کن

## ۲-۱) بازی به‌روز است؟

فایل `index.html` را از گیت‌هاب دانلود کن و در پوشهٔ زیر جایگزین کن:
```
app/src/main/assets/index.html
```

## ۲-۲) درگاه پرداخت هست؟

`MainActivity.kt` را باز کن، `Ctrl+F` بزن و کلمهٔ زیر را جستجو کن:
```
IabHelper
```
- **پیدا شد** ✅ برو مرحلهٔ بعد
- **پیدا نشد** ❌ محتوای `MainActivity-با-پرداخت.kt` را از گیت‌هاب کپی کن و کل
  فایل `MainActivity.kt` را با آن جایگزین کن

> ⚠️ فقط **خط اول** (`package ...`) را از فایل خودت نگه دار.

## ۲-۳) اصلاح اندروید ۱۵ هست؟

در همان فایل `Ctrl+F` بزن و جستجو کن:
```
WindowInsetsCompat
```
- **پیدا شد** ✅
- **پیدا نشد** ❌ فایل تازه را از گیت‌هاب بردار (بدون آن، کارت‌های پایین روی
  گوشی‌های جدید زیر نوار ناوبری می‌روند)

---

# بخش ۳ — ساخت APK

۱. `Build` ← `Clean Project` → صبر کن
۲. `Build` ← `Generate Signed App Bundle / APK...`
۳. **APK** را انتخاب کن (نه App Bundle) ← `Next`
۴. کلید (keystore) خودت را بده ← `Next`
۵. **release** را انتخاب کن ← `Create`
۶. صبر کن تا پیام سبز بیاید

## چک نهایی قبل از آپلود
در پوشهٔ `app/release/` فایل **`output-metadata.json`** را با Notepad باز کن.
باید این دو خط را ببینی:

```
"applicationId": "com.smahdi2razavi.pasur11"
"versionCode": 2
```

---

# بخش ۴ — پنل مایکت

## ۴-۱) عنوان کالای دوم را درست کن
`vip_subscription_2` الان «بسته یک ماه VIP» است ولی بازی **دو ماهه**
می‌فروشد. عوضش کن به:

| شناسه | عنوان درست |
|---|---|
| `vip_subscription` | اشتراک ویژه — یک ماه |
| `vip_subscription_2` | اشتراک ویژه — دو ماه |
| `vip_subscription_3` | اشتراک ویژه — سه ماه |

## ۴-۲) مطمئن شو هر شش کالا فعال‌اند
دکمهٔ وضعیت هر کدام باید روشن (آبی) باشد.

---

# بخش ۵ — آپلود و ارسال

۱. فایل `app/release/app-release.apk` را آپلود کن
۲. تیک **«خرید درون‌برنامه‌ای»** را بزن
۳. متن توضیحات را از فایل `متن-توضیحات-مایکت.md` کپی کن
۴. دسته: **بازی ← کارتی**
۵. **ارسال برای بررسی**

---

# اگر رد شد

دلیل رد شدن را از منوی **⋮** کنار نسخه بخوان و برایم بفرست.

دفعهٔ قبل دلیلش «درگاه پرداخت وصل نبود» بود. اگر بخش ۲-۲ را درست انجام
داده باشی، این بار مشکلی نیست.

---

# ❓ دربارهٔ اسم SMRx

APK فعلی‌ات `com.smahdi2razavi.pasur11` است و **همین آمادهٔ آپلود است**.

اگر حتماً `com.SMRx.pasur11` می‌خواهی، باید:
1. برنامه را در مایکت حذف کنی
2. برنامهٔ جدید با آن نام بسازی
3. هر شش کالا را از نو بسازی
4. کلید RSA جدید را در `MainActivity.kt` جایگزین کنی
5. `applicationId` را در gradle عوض کنی و دوباره بسازی

**این نام را هیچ کاربری نمی‌بیند.** پیشنهاد من: اول با همین اسم منتشر کن؛
اگر بعداً خواستی، آن موقع عوضش کن.

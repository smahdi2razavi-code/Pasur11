# اتصال بازی به سرور (Firebase) — ۰ تا ۱۰۰

## سرور یعنی چی و چرا Firebase؟
«سرور» جایی است که اطلاعات آنلاین بازی نگه‌داری و بین کاربران رد و بدل می‌شود.
به‌جای اینکه خودت یک سرور بخری و کد سرور بنویسی و نگه‌داری کنی (سخت و هزینه‌بر)،
از **Firebase** استفاده می‌کنیم: یک سرور آمادهٔ **رایگان** از گوگل که **هیچ کد
سروری لازم ندارد** — فقط چند تنظیم و یک آدرس.

با وصل‌شدن Firebase، این سه چیز **یکجا فعال می‌شوند**:
- ✅ بازی دو نفرهٔ آنلاین (با کد کوتاه)
- ✅ جدول رتبه‌بندی جهانی + یکتایی نام‌کاربری
- ✅ ربات تلگرام مدیریت کاربران (مسدودی/سکه/VIP با شناسه)

---

## گام ۱ — ساخت پروژهٔ Firebase
1. برو به: https://console.firebase.google.com
2. با حساب گوگل وارد شو → **Add project / افزودن پروژه**.
3. یک اسم بگذار (مثلاً `pasur11`) → Continue.
4. Google Analytics را می‌توانی خاموش کنی (Disable) → **Create project** → صبر کن.

## گام ۲ — ساخت دیتابیس (Realtime Database)
1. از منوی چپ: **Build → Realtime Database**.
2. **Create Database** را بزن.
3. محل سرور: **Belgium (europe-west1)** یا **Singapore** را انتخاب کن (به ایران نزدیک‌تر/پایدارتر).
4. حالت شروع: **Start in test mode** → Enable.

## گام ۳ — تنظیم قوانین دسترسی (Rules)
1. در همان صفحهٔ Realtime Database، بالای صفحه تب **Rules** را بزن.
2. محتوا را با این جایگزین کن و **Publish** را بزن:
```json
{
  "rules": {
    ".read": true,
    ".write": true
  }
}
```
> این قوانین ساده و باز است تا بازی راحت کار کند. برای شروع و انتشار کافی است؛
> بعداً هر وقت خواستی می‌توانیم امن‌ترش کنیم.

## گام ۴ — برداشتن آدرس دیتابیس
بالای صفحهٔ Realtime Database، یک آدرس می‌بینی شبیه این:
```
https://pasur11-xxxx-default-rtdb.europe-west1.firebasedatabase.app
```
این آدرس را **کپی** کن (همین «آدرس سرور» توست).

## گام ۵ — گذاشتن آدرس در بازی (تنها تغییر کد)
1. فایل `index.html` را باز کن.
2. این خط را پیدا کن:
```js
const FIREBASE_DB_URL='YOUR_FIREBASE_DB_URL';
```
3. به‌جای `YOUR_FIREBASE_DB_URL` آدرس خودت را بگذار (بدون `/` آخر). مثال:
```js
const FIREBASE_DB_URL='https://pasur11-xxxx-default-rtdb.europe-west1.firebasedatabase.app';
```
4. ذخیره کن. همین! سمت بازی تمام شد. ✅

## گام ۶ — اعمال در اپ
`index.html` تازه را در `app/src/main/assets` جایگزین کن → `Build → Clean Project` →
اپ را از گوشی پاک کن → **Run**. حالا:
- بازی دو نفره با «کد کوتاه» کار می‌کند،
- جدول رتبه‌بندی جهانی پر می‌شود،
- ربات تلگرام (طبق `telegram-bot/راهنمای-ربات.md`) می‌تواند کاربران را کنترل کند.

---

## دربارهٔ «کد سرور»
چون از Firebase استفاده می‌کنیم، **کد سروری برای نوشتن و نگه‌داری وجود ندارد** —
این بهترین حالت برای توست (رایگان، بدون دردسر، بدون هزینهٔ ماهانه). تنها «کد»یی که
سمت سرور اجرا می‌شود، همان ربات تلگرام است که قبلاً آماده کرده‌ایم و آن هم با همین
Firebase کار می‌کند.

> اگر روزی خواستی سرور اختصاصی خودت (Node.js) داشته باشی، آن هم شدنی است ولی
> هزینه و نگه‌داری دارد و برای این بازی لازم نیست. Firebase برای هزاران کاربر رایگان کافی است.
